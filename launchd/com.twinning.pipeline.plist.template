<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.twinning.pipeline</string>

  <key>ProgramArguments</key>
  <array>
    <string>/bin/bash</string>
    <string>__HARNESS_ROOT__/bin/run-local.sh</string>
  </array>

  <key>WorkingDirectory</key>
  <string>__TARGET_REPO__</string>

  <!-- Fire every 5 minutes, coalesced across sleep. -->
  <key>StartInterval</key>
  <integer>300</integer>

  <!-- Run once on login so we don't wait up to 5 min after a reboot. -->
  <key>RunAtLoad</key>
  <true/>

  <!-- Don't flood the system if we crash in a loop. -->
  <key>ThrottleInterval</key>
  <integer>60</integer>

  <key>StandardOutPath</key>
  <string>__HARNESS_STATE_DIR__/logs/launchd.out.log</string>

  <key>StandardErrorPath</key>
  <string>__HARNESS_STATE_DIR__/logs/launchd.err.log</string>

  <key>EnvironmentVariables</key>
  <dict>
    <key>PATH</key>
    <string>/opt/homebrew/bin:/opt/homebrew/sbin:/usr/local/bin:/usr/local/sbin:/usr/bin:/bin:/usr/sbin:/sbin</string>
    <key>HOME</key>
    <string>__HOME__</string>
    <key>TARGET_REPO</key>
    <string>__TARGET_REPO__</string>
    <key>HARNESS_STATE_DIR</key>
    <string>__HARNESS_STATE_DIR__</string>
  </dict>
</dict>
</plist>
